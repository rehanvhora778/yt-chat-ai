# Base URL of the backend API.
# In development you can leave it empty and rely on the Vite proxy (/api).
# In production set it to your deployed backend, e.g. https://api.example.com
VITE_API_BASE_URL=
